# ==================== ЭТАП 4: ОСНОВНЫЕ КОМАНДЫ ====================

# Информация о VFS
vfs-info

# Навигация по корневой директории
ls /

# Тестирование команды cat
cat /readme.txt
cat /documents/file1.txt

# Тестирование команды head
head -n 3 /readme.txt
head -n 5 /documents/file2.txt
head /documents/file1.txt

# Тестирование команды cd
cd documents
ls
cd ../images
ls
cd /

# Тестирование обработки ошибок этапа 4
cat /nonexistent_file.txt
head -n abc /readme.txt
cd nonexistent_directory

# ==================== ЭТАП 5: ДОПОЛНИТЕЛЬНЫЕ КОМАНДЫ ====================

# Тестирование команды chmod
chmod 755 /documents
chmod 644 /readme.txt
chmod 600 /documents/file1.txt

# Тестирование команды rmdir
# Попытка удалить непустую директорию
rmdir /documents
rmdir /images

# Тестирование обработки ошибок этапа 5
chmod 888 /documents
chmod 75 /readme.txt
chmod 644 /nonexistent_file
rmdir /nonexistent_dir
rmdir /

# Тестирование граничных случаев
head -n 0 /readme.txt
head -n 100 /documents/file1.txt

# Комбинированное использование команд
cd documents
chmod 755 .
ls
cat file1.txt
cd ../images
chmod 700 .
ls
cd /

# Финальная проверка состояния VFS
vfs-info
ls /

# Завершение работы
exit